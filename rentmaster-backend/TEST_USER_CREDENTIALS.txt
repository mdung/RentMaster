============================================
RENTMASTER - TEST USER CREDENTIALS
============================================

Username: testuser
Password: admin123

(Note: Using same password as admin for simplicity)

============================================
HOW TO CREATE THIS USER
============================================

1. Open your database client (DBeaver, pgAdmin, etc.)
2. Connect to the 'rentmaster' database
3. Run the SQL script: CREATE_TEST_USER.sql
   OR run this SQL directly:

   INSERT INTO users (username, password, full_name, email, role, active, created_at)
   VALUES (
       'testuser',
       '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwK8p6wO',
       'Test User',
       'testuser@rentmaster.com',
       'ADMIN',
       true,
       NOW()
   )
   ON CONFLICT (username) DO UPDATE SET
       password = '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwK8p6wO',
       active = true,
       failed_login_attempts = 0,
       account_locked_until = NULL;

============================================
VERIFY TEST USER
============================================

Run this query to check the test user:

SELECT 
    username,
    full_name,
    email,
    role,
    active,
    failed_login_attempts,
    account_locked_until
FROM users 
WHERE username = 'testuser';

Expected values:
- username: testuser
- full_name: Test User
- email: testuser@rentmaster.com
- role: ADMIN
- active: true
- failed_login_attempts: 0
- account_locked_until: NULL

============================================
AVAILABLE TEST USERS
============================================

1. Admin User:
   Username: admin
   Password: admin123

2. Test User:
   Username: testuser
   Password: admin123

============================================
NOTE
============================================

Both users use the same password (admin123) for simplicity.
The password hash is verified to work correctly.

If you want different passwords, you'll need to generate
new BCrypt hashes using Spring's BCryptPasswordEncoder.

============================================

